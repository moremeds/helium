# Phase 2.5a exit evidence — Ops Phases B-C
#
# The operations substrate spans Ops Phases A-D, all of which are program phase
# P2.5a. This manifest is the single P2.5a record and is appended to as each
# Ops phase gate is reached. The first seven rows are Ops Phase B's; the six
# appended rows are Ops Phase C's.
#
# Validated against EvidenceManifestSchema by
# packages/core/tests/evidence-manifest.spec.ts, alongside the P0 and P1
# manifests.
#
# WHERE THE HASHES COME FROM
# --------------------------
#   run:    https://github.com/moremeds/helium/actions/runs/33244380149
#   commit: 3b54957  (feat/ops-phase-b)
#   node:   22.19.0 (pinned, .github/workflows/ci.yml:21,49)
#   pnpm:   11.24.0   vitest: 3.2.7
#
# Ops Phase C:
#   run:    https://github.com/moremeds/helium/actions/runs/33262612425
#   commit: 4b2b57b  (feat/ops-phase-c)
#   node:   22.19.0 (pinned, .github/workflows/ci.yml:21,70)
#   pnpm:   11.24.0   vitest: 3.2.7
#
# A hash captured on a developer machine is not admissible. Captured output
# carries wall-clock durations, so re-running the same commit yields a
# different hash; the hash pins this run, and `reproduction` re-runs the
# assertion rather than the hash.
#
# WHAT IS DELIBERATELY NOT CLAIMED
# --------------------------------
# Every row here is offline. No SOP has been certified against a live host, no
# drill has run against a real component, and nothing has been deployed. The
# live-drill gate is recorded as its own BLOCKED claim rather than left
# unstated, because a manifest that lists only what passed reads as
# completeness.
manifestVersion: p0-1
phase: P2.5a
scope: offline
recordedAt: 2026-08-29T16:20:59Z
claims:
  - id: P2.5A-CRASH-MATRIX
    assertion: "A controller terminated at any point in a recovery attempt produces no duplicate attempt, receipt or terminal state, and never blindly retries."
    acceptanceBound: "For all eight crash points in the attempt sequence, and for a torn-write variant of each: no duplicate event id survives a restart, at most one terminal action state exists, at most one receipt is recorded, at most one lease is ever active across repeated re-acquires, and every reconciliation decision has rerun=false."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,49); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/test.log"
        sha256: "67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
    baseline: "v1 behavior at 72a48265b44ed0de7d204bd82cc766aedbaf81d8"
    reproduction: "pnpm exec vitest run --project unit packages/core/tests/operations-crash-matrix.spec.ts"
    failures: "None. The matrix truncates a real durable log rather than discarding in-memory state, so each case asks what a restarted process can conclude from what actually reached disk."
    status: PROVEN
    limitation: "Simulates termination by truncating the log, not by killing a live daemon under load. It proves the replay contract, not the behaviour of the host process under signals."
    nextGate: "Ops Phase C: collector, probes and adapters."

  - id: P2.5A-ATTRIBUTION-NEVER-AUTOMATIC
    assertion: "A controller never claims its own action succeeded merely because the target later became healthy."
    acceptanceBound: "Across the full attribution matrix: a non-zero exit with passing postconditions is uncertain and never automatic; an operator intervention during an in-flight action yields superseded-by-operator with operator attribution whatever the postconditions say; and the production-derived incident fixture attributes to the operator with automation FAILED."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,49); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/test.log"
        sha256: "67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
    baseline: "The read-only mini audit of 2026-08-25, design sections 3.1 and 3.6."
    reproduction: "pnpm exec vitest run --project unit packages/core/tests/operations-verify.spec.ts packages/core/tests/operations-reducer.spec.ts"
    failures: "None. A missing receipt with a recorded intent is uncertain rather than external, so an external actor is never credited for a mutation Helium may have performed."
    status: PROVEN
    limitation: "Proves the classifier against recorded evidence shapes. It does not prove the daemon assembles that evidence correctly from a live host, which needs the collector in Ops Phase C."
    nextGate: "Ops Phase C: collector, probes and adapters."

  - id: P2.5A-NOT-NEEDED-NO-AUTOMATION-CREDIT
    assertion: "An action whose pre-action baseline already passed is excluded from every automation-credit statistic."
    acceptanceBound: "A baseline with all postconditions passing terminates the action not-needed before any spawn; automation credit is granted only to a genuine succeeded; and succeeded is unreachable from an all-passing baseline under every combination of receipt, postcondition verdict and intent."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,49); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/test.log"
        sha256: "67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
    baseline: "v1 behavior at 72a48265b44ed0de7d204bd82cc766aedbaf81d8"
    reproduction: "pnpm exec vitest run --project unit packages/core/tests/operations-sop.spec.ts packages/core/tests/operations-verify.spec.ts"
    failures: "None. Without this rule an operator fixing the component concurrently hands the controller a free exit 0 plus passing postconditions, feeding the promotion gate exactly the case it exists to catch."
    status: PROVEN
    limitation: "Proves the exclusion in the classifier and the schema. The promotion gate that consumes the statistic does not exist until P4."
    nextGate: "P4: bounded production window and promotion."

  - id: P2.5A-MUTATION-OWNERSHIP-FAIL-CLOSED
    assertion: "No mutation is attempted while component ownership is competing, external, absent, or unverifiable."
    acceptanceBound: "canMutate returns ok only for owner opsd with a clear probe; a timeout, non-zero exit, truncated output and unparseable output each yield unknown and therefore refusal; and at every prefix of the handoff and rollback sequences at most one mutation controller is enabled, with a crash between disable and enable leaving zero."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,49); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/test.log"
        sha256: "67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
    baseline: "Design section 13.2."
    reproduction: "pnpm exec vitest run --project unit packages/core/tests/operations-mutation-owner.spec.ts plugins/ops-agent/src/probes/launchd-controller.test.ts"
    failures: "None. A partially readable controller table is refused entirely, because a label the parser skipped is exactly the rival it exists to find."
    status: PARTIAL
    limitation: "Every probe result comes from the fake runner. No test invokes the real service-manager binary, by design, so the parser is proven against the table shape this program believes the host emits -- not against that host's actual output. Closing this needs a read-only capture from the mini, which the AC#1 window forbids until 2026-08-31."
    nextGate: "Ops Phase C Task 10: collector and host probes."

  - id: P2.5A-NO-COMMAND-STRING
    assertion: "No certified action is ever expressed, stored or executed as a command string."
    acceptanceBound: "The SOP, registry and intent schemas reject a command or shell key; the executor spawns with shell:false and its source contains no shell invocation; a shell metacharacter argument reaches the child as a literal; and every off-schema argv is refused before anything is spawned."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,49); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/test.log"
        sha256: "67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
    baseline: "v1 behavior at 72a48265b44ed0de7d204bd82cc766aedbaf81d8"
    reproduction: "pnpm exec vitest run --project unit plugins/ops-agent/src/script-registry.test.ts plugins/ops-agent/src/script-executor.test.ts"
    failures: "None. The timeout test proves process-group reaping rather than orphaning: a descendant rewrites a marker every 25ms, and after the deadline the marker is deleted and must not reappear."
    status: PROVEN
    limitation: "Proven against a fake repair script. No certified production script has been registered or run."
    nextGate: "Ops Task 16: inventory and certify the first existing scripts."

  - id: P2.5A-RECOVERY-BUNDLE-COMPLETE
    assertion: "A recovery claim cannot be recorded without the evidence chain that makes it checkable, and absent evidence is declared rather than omitted."
    acceptanceBound: "The recovery bundle refuses omission of hashed observations, incident snapshot, SOP digest, signed authority manifest entry, mutation ownership, controller probe, postcondition samples, verifier or replay reference; refuses to omit intent, receipt or lease without a notApplicableReason; refuses a succeeded outcome with no receipt or an all-passing baseline; and reuses the canonical five-status vocabulary."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,49); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/test.log"
        sha256: "67058c15c9845cf61aff25b8c495358c595212bebfa4ddb01f4dfa2dda32aaa6"
    baseline: "The canonical EvidenceBundle from multi-agent Task 7."
    reproduction: "pnpm exec vitest run --project unit packages/core/tests/recovery-evidence-bundle.spec.ts"
    failures: "None."
    status: PROVEN
    limitation: "Proves the schema refuses incomplete bundles. No bundle has yet been produced from a real recovery, because no recovery has been performed."
    nextGate: "Ops Phase C: collector, probes and adapters."

  - id: P2.5A-LIVE-DRILL
    assertion: "A certified SOP restores a real component's integrity, freshness and coverage under a controlled drill."
    acceptanceBound: "The corrupt fixture and a controlled drill both pass integrity, freshness and coverage, against a real component on a real host."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "not yet runnable: no drill harness and no certified production SOP exist"
      toolVersion: "n/a"
      decision: inconclusive
    artifacts: []
    baseline: "Design section 3.6, which already records this assertion as BLOCKED."
    reproduction: "Blocked. Requires a certified SOP, a drill harness, and a host the AC#1 observation window permits touching."
    failures: "Not attempted. Recording this as anything other than BLOCKED would imply a drill happened."
    status: BLOCKED
    limitation: "Nothing about targeted repair is proven. The design recorded this as BLOCKED before any code existed and it remains BLOCKED: no drill has run, no production SOP is certified, and no mutation has been performed on any host."
    nextGate: "Ops Task 16 and 17, then the post-AC#1 drill gate."

  - id: P2.5A-OPS-CONFIG-TOPOLOGY
    assertion: "Ops configuration loads into an atomic, dependency-aware, effect-scoped registry, and the collector remains structurally isolated from executors and leases."
    acceptanceBound: "Bounded tenant-scoped YAML loading includes components, checks, SOPs and dependency edges; a bad or cross-bundle reference installs nothing and cannot leave a dangling graph or certified SOP; and the topology contract resolves every workspace package import before refusing any collector path to an executor or the core lease target."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test && pnpm run test:contracts"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,70); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:c7d2879e99a6838ff8be631ddb403df71487c58bfa53dd9356ef9ddf8118e1a0"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/phase-c-test.log"
        sha256: "53592d1744c6839379632018950882255898ef665f0e5484f91dafe0b7d60573"
      - path: "docs/evidence/p2.5a/phase-c-test-contracts.log"
        sha256: "a31bf5155dd4f7b92a109735d89de10bb48e0e478d4b0cc0af81012c3b79b5b6"
    baseline: "Ops Phase B merge 7cae01e, which supplied the core operations contracts but no configuration loader or collector topology."
    reproduction: "pnpm exec vitest run --project unit plugins/ops-agent/src/bundle-loader.test.ts plugins/ops-agent/src/component-registry.test.ts && pnpm exec vitest run --project contracts contracts/tests/topology-structure.contract.spec.ts contracts/tests/core-neutrality.contract.spec.ts"
    failures: "None. The cross-bundle cases reject incoming dependencies and SOP references before commit, then prove the original bundle can still be disposed without corrupting the remaining graph."
    status: PROVEN
    limitation: "Proven against committed YAML and static source traversal in CI. It does not prove configuration reload or disposal inside a deployed long-running daemon."
    nextGate: "Ops Phase D: compose the standalone controller and adversarial process contracts."

  - id: P2.5A-RAW-OBSERVATION-REFS
    assertion: "Every runnable Phase C host probe fails closed and preserves the runner-supplied raw command references needed to replay its observation or mutation-admission decision."
    acceptanceBound: "Process, disk, macOS resource and controller probes use fixed argv with individual timeouts; required timeout or unparseable input yields unknown; memory pressure, CPU busy time and bounded process contribution are interpreted into dimensions declared by the host ComponentSpec; and the exact controller check passed to canMutate retains an evidenceRef that RecoveryEvidenceSchema requires."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,70); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:53592d1744c6839379632018950882255898ef665f0e5484f91dafe0b7d60573"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/phase-c-test.log"
        sha256: "53592d1744c6839379632018950882255898ef665f0e5484f91dafe0b7d60573"
    baseline: "Ops Phase B probe and evidence interfaces, before runnable resource collection and raw-reference propagation existed."
    reproduction: "pnpm exec vitest run --project unit plugins/ops-agent/src/probes/process.test.ts plugins/ops-agent/src/probes/disk.test.ts plugins/ops-agent/src/probes/macos-resource.test.ts plugins/ops-agent/src/probes/launchd-controller.test.ts packages/core/tests/recovery-evidence-bundle.spec.ts"
    failures: "None. Every fail-closed branch carries the reference returned by the same fake command invocation rather than manufacturing a second reference."
    status: PROVEN
    limitation: "The command shapes are authored fixtures and the runners are fakes. This proves propagation after a runner supplies an evidenceRef; it does not prove that a production runner or artifact store persisted the referenced raw bytes."
    nextGate: "Post-AC#1 read-only host capture, then Ops Phase D controller composition."

  - id: P2.5A-INITIAL-ADAPTERS
    assertion: "The six initial component adapters emit schema-valid, dimension-specific observations without discarding caller-supplied raw evidence references."
    acceptanceBound: "Livewire, Argon, Apex, Colima, PostgreSQL and Helium adapters preserve independent health dimensions, keep a healthy listener from hiding stale or corrupt data and backup state, enumerate expected tenants, and preserve the raw evidenceRefs supplied with every source sample."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,70); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:53592d1744c6839379632018950882255898ef665f0e5484f91dafe0b7d60573"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/phase-c-test.log"
        sha256: "53592d1744c6839379632018950882255898ef665f0e5484f91dafe0b7d60573"
    baseline: "The six sanitized production-derived fixture families from Ops Phase A."
    reproduction: "pnpm exec vitest run --project unit plugins/ops-agent/src/adapters/livewire.test.ts plugins/ops-agent/src/adapters/argon.test.ts plugins/ops-agent/src/adapters/apex.test.ts plugins/ops-agent/src/adapters/colima.test.ts plugins/ops-agent/src/adapters/postgres.test.ts plugins/ops-agent/src/adapters/helium.test.ts"
    failures: "None. Adapter tests assert that caller evidence references survive unchanged instead of being replaced with adapter-generated artifact names."
    status: PROVEN
    limitation: "Proven against authored and sanitized fixture inputs only. No adapter connected to a live service, database, mount, or credential in this run."
    nextGate: "Post-AC#1 read-only capture and Ops Phase D controller integration."

  - id: P2.5A-ALERT-ADMISSION-PURE
    assertion: "Alert transitions and host-pressure admission are deterministic without a model or team controller in the safety path."
    acceptanceBound: "Alerts honor sustained-for windows, dedupe an open incident, inhibit symptoms, emit recovery once, alert again on a later recurrence, reset an interrupted window and surface delivery failure; under sustained pressure the pure admission function refuses optional teams and subagent fan-out while admitting collectors, deterministic actions, one minimal incident lane and dead-man work."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm run test"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,70); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:53592d1744c6839379632018950882255898ef665f0e5484f91dafe0b7d60573"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/phase-c-test.log"
        sha256: "53592d1744c6839379632018950882255898ef665f0e5484f91dafe0b7d60573"
    baseline: "Ops Phase B incident and observation projections, before alert transitions or resource admission policy existed."
    reproduction: "pnpm exec vitest run --project unit packages/core/tests/operations-admission.spec.ts plugins/ops-agent/src/alerts.test.ts"
    failures: "None. Recovery before the for-window matures clears the timer, so a later recurrence must satisfy a fresh complete window."
    status: PROVEN
    limitation: "Admission is deliberately a pure function in Phase C; it is not enforced by a team controller until P3.5 Task 13b. Alerts use a fake delivery channel and have not been emitted from a deployed collector."
    nextGate: "P3.5 Task 13b for enforcement; Ops Phase D for controller-side alert integration."

  - id: P2.5A-PHASE-C-OFFLINE-GATE
    assertion: "The complete observe-only Phase C tree builds, typechecks, and passes the repository unit, contract and local end-to-end gates at the pinned runtime."
    acceptanceBound: "At commit 4b2b57b under Node 22.19.0: every workspace build and typecheck passes, 755 unit tests pass, 55 contract tests pass with the one live-provider contract explicitly skipped, and both local end-to-end modes pass."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "pnpm build && pnpm typecheck && pnpm test && pnpm test:contracts && pnpm test:e2e-local"
      toolVersion: "node 22.19.0 (pinned in .github/workflows/ci.yml:21,70); pnpm 11.24.0; vitest 3.2.7"
      outputHash: "sha256:67496972d596b349e810aee9341bb0d1d3706812eeadb707abb98475027f4dde"
      decision: pass
    artifacts:
      - path: "docs/evidence/p2.5a/phase-c-build.log"
        sha256: "ec26d8592a73520d07748ecaf8adac017e878b7dab9d55b891f064a25083a2cf"
      - path: "docs/evidence/p2.5a/phase-c-typecheck.log"
        sha256: "f361a9f9894c2212b48dd5d1a195ed92de29e44d32dcba407b0bdfca9ae2cf94"
      - path: "docs/evidence/p2.5a/phase-c-test.log"
        sha256: "53592d1744c6839379632018950882255898ef665f0e5484f91dafe0b7d60573"
      - path: "docs/evidence/p2.5a/phase-c-test-contracts.log"
        sha256: "a31bf5155dd4f7b92a109735d89de10bb48e0e478d4b0cc0af81012c3b79b5b6"
      - path: "docs/evidence/p2.5a/phase-c-test-e2e-local.log"
        sha256: "56866f2ac667fff6e84ba9550f6fa117c62a8988f31e2b8796c8fe00cc5aa66c"
    baseline: "Ops Phase B merge 7cae01e and the Phase C gate in the authoritative Ops implementation plan."
    reproduction: "pnpm build && pnpm typecheck && pnpm test && pnpm test:contracts && pnpm test:e2e-local && git diff --check"
    failures: "None in CI run 33262612425. The live provider contract remained explicitly skipped because CI carries no provider credentials."
    status: PROVEN
    limitation: "The end-to-end engines are local stubs, one live-provider contract is skipped, and neither opsd nor an action controller exists in Phase C. No byte was installed on the mini and no real host command ran."
    nextGate: "Ops Phase D: observe/suggest controller, script certification, adversarial contracts and reversible packaging."

  - id: P2.5A-LIVE-HOST-OBSERVATION
    assertion: "The real mini's resource, controller and component output can be collected read-only into replayable observations without altering host state."
    acceptanceBound: "After AC#1 closes and separate observation approval is recorded, collect every initial component and host dimension with the exact production runner, persist raw bytes and references, parse without UNKNOWN caused by shape drift, and demonstrate zero writes, process starts, service-manager changes or mutations."
    assertionClass: deterministic
    evidencePolicyVersion: p0-1
    verification:
      verifier: command
      command: "not yet runnable: AC#1 forbids touching the mini before 2026-08-31 and no production runner is deployed"
      toolVersion: "n/a"
      decision: inconclusive
    artifacts: []
    baseline: "The read-only mini audit of 2026-08-25 and the AC#1 freeze boundary in design section 13.4."
    reproduction: "Blocked until AC#1 evidence is recorded and a separate read-only observation plan is approved; Phase C itself must not run even a manual one-shot probe on the mini."
    failures: "Not attempted. Fixture-backed parser success is not evidence that the live host emits the same shapes or that a production runner persists the referenced bytes."
    status: BLOCKED
    limitation: "No Phase C probe or adapter has run against the mini. This row prevents the offline green gate from being misread as live observation proof."
    nextGate: "After 2026-08-31: record AC#1 completion, approve a read-only capture plan, and run the production observation gate before any install."

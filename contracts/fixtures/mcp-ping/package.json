{
  "name": "mcp-ping",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Spike B fixture: stdio MCP server that proves a claude -p child can reach a helium tool.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
